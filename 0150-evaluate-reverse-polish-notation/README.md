In this problem, I decided to use stack, we store numbers in the stack, the moment we encounter an operation we pop the numbers to perform the operation and store the result back in the stack. If we have division we do integer division. 

For operation mapping I decided to use hash map instead of several if statements.