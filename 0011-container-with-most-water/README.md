Since we can't lose information about indices sorting is not working here. First of all we need to figure out the formula of an area with two fixed walls, which is `(j-k)* min(heights[j], heights[k])`

Then if the height of one pointer is smaller then we move it